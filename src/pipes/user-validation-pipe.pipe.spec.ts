import { UserValidationPipePipe } from './user-validation-pipe.pipe';

describe('UserValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new UserValidationPipePipe()).toBeDefined();
  });
});
